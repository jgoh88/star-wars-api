const errorList = {
    NOT_IN_GLOBALPROVIDER: "You must use inside GlobalStateContext.Provider",
    INVALID_DATA_ACTION: "Invalid dispatch action type for data",
    FETCH_ERROR: "Error encountered while getting data from SWAPI",
}

export default errorList