import NavBar from "./components/NavBar";

const { BrowserRouter, Routes } = require("react-router-dom");

function App() {
  return (
    <BrowserRouter>
      <NavBar />
      <Routes>
        
      </Routes>
    </BrowserRouter>
  );
}

export default App;
